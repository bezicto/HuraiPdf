{
    "name": "bezicto/hurai-pdf",
    "description": "A zero-runtime-dependency, pure-PHP PDF text extraction library.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.3 <8.6",
        "ext-ctype": "*",
        "ext-openssl": "*",
        "ext-zlib": "*"
    },
    "suggest": {
        "ext-iconv": "Improves legacy PDF font encoding conversion when mbstring is unavailable.",
        "ext-mbstring": "Recommended for PDF font encoding conversion."
    },
    "autoload": {
        "psr-4": {
            "HuraiPdf\\": "src/HuraiPdf/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
